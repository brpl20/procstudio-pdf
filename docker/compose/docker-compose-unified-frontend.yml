# Example Docker Compose for Unified ProcStudio PDF Container
# MODE=FRONTEND: Frontend only, connects to separate backend

services:
  procstudio-pdf-backend:
    container_name: ProcStudio-PDF-Backend
    build:
      context: ../..
      dockerfile: docker/Dockerfile.unified
    ports:
      - "8081:8080"
    volumes:
      - ./procstudio/data:/usr/share/tessdata:rw
      - ./procstudio/config:/configs:rw
      - ./procstudio/logs:/logs:rw
      - ./procstudio/customFiles:/customFiles:rw
      - ./procstudio/pipeline:/pipeline:rw
    environment:
      MODE: BACKEND
      DISABLE_ADDITIONAL_FEATURES: "false"
      DOCKER_ENABLE_SECURITY: "false"
      PUID: 1000
      PGID: 1000
      UMASK: "022"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/api/v1/info/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 4G

  procstudio-pdf-frontend:
    container_name: ProcStudio-PDF-Frontend
    build:
      context: ../..
      dockerfile: docker/Dockerfile.unified
    ports:
      - "8080:8080"
    environment:
      MODE: FRONTEND

      # Point to the backend service
      VITE_API_BASE_URL: http://procstudio-pdf-backend:8080

      # Minimal config needed for frontend
      PUID: 1000
      PGID: 1000
    depends_on:
      - procstudio-pdf-backend
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
